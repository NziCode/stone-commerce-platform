# بسته ورود اخبار سنگ

این بسته شامل ۳۰ خبر چندزبانه (فارسی، انگلیسی، ایتالیایی، عربی، هندی، چینی و ترکی) است. متن‌ها原创 و مناسب صنعت سنگ هستند و از کپی مستقیم محتوای سایت‌های دیگر استفاده نمی‌کنند.

## اجرای روی سرور

۱. فایل `database/sql/pedrini_news_seed.sql` را روی سرور کپی کنید.

۲. این فایل را فقط روی دیتابیس MySQL پروژه اجرا کنید:

```bash
mysql -u DB_USER -p DB_NAME < database/sql/pedrini_news_seed.sql
```

یا محتوای فایل را در phpMyAdmin اجرا کنید. اسکریپت قبل از درج، فقط خبرهایی را که slug آن‌ها با `stone-industry-news-` شروع می‌شود حذف می‌کند؛ سایر خبرها دست‌نخورده می‌مانند.

## فایل‌های لازم برای تصاویر

برای تصاویر خبر، این پوشه را هم کپی کنید:

```text
public/assets/images/about-default.jpg
public/assets/images/hero-default.jpg
```

در صورتی که دیسک عمومی پروژه در `storage/app/public` تنظیم شده است، نیازی به اجرای Artisan برای این دو تصویر نیست؛ اسکریپت به مسیر `images/...` اشاره می‌کند. برای تنوع بیشتر می‌توانید تصاویر دارای مجوز خودتان را با همین نام‌ها جایگزین کنید.

## پوشه‌هایی که لازم نیست کپی شوند

پوشه‌های `vendor`، `node_modules` و `public/build` برای این ورود اطلاعات لازم نیستند؛ مگر این‌که نسخه فرانت‌اند سرور از قبل build نشده باشد.

